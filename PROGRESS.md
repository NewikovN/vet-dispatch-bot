# PROGRESS.md — текущая стадия проекта

> Обновлять после каждого крупного блока. Это точка входа в контекст для нового чата.

## Где мы сейчас
Переходим с VK-прототипа на боевую реализацию для MAX. Ядро (домен + БД) переписывается под новые требования заказчика. MAX-адаптер ещё не начат.

## Транспорт MAX — проверено и работает
- Токен бота живой, домен `platform-api2.max.ru` отвечает.
- Сертификат Минцифры (`certs/russian_trusted_root.pem`) подключён через `NODE_EXTRA_CA_CERTS`, TLS проходит.
- Библиотека `@maxhub/max-bot-api` установлена.
- Бот: имя «Мокрый Нос», прошёл модерацию.

## Готово (ядро)
- Модели, роли, права (`domain/models.ts`) — обновлены под статус `approved`, добавлены `canApprove`, `canReject`, `canSeeDetails`.
- Жизненный цикл заявки `open → taken → approved → closed` + отклонение.
- Репозиторий заявок: создание, атомарный захват, одобрение, отклонение, закрытие (`db/requestsRepo.ts`).
- Схема БД: заявки (+ `approved_at`, `manage_message_id`), пользователи, `city_chats` (два чата на город).
- Репозиторий чатов по городам (`db/cityChatsRepo.ts`).
- Порт `Messenger`: добавлены `sendManageCard`, `editManageCard`.
- Сервис `requestService.ts`: `publishRequest` (в рабочий чат по городу), `takeRequest` (захват без контактов + карточка в управленческий чат), `approveTake`, `rejectTake`.
- Тест атомарного захвата (`test-claim.ts`) — проходит.

## В работе
- Обновить закрытие заявки `finishClosing` под новый цикл (`approved → closed`) и обновление обеих карточек.
- Проверить, что весь домен компилируется (`app.ts` сейчас с ошибками — это VK-слой, будет заменён MAX-адаптером).

## Дальше (не начато)
- MAX-адаптер: `adapters/max/MaxAdapter.ts`, `adapters/max/cardView.ts`, точка входа под события MAX (`bot_started`, `message_callback`, личные сообщения).
- Команды директора для привязки чатов к городам.
- Экспорт заявок в Excel для директора (`exceljs`).
- Деплой: VPS Beget, nginx, certbot, сертификат Минцифры на сервере, webhook, systemd, бэкапы.

## Важные решения (не переигрывать без причины)
- Один бот на все города (лимит MAX — 5 ботов на организацию; «бот на город» отвергнут).
- Два чата на город вместо личек управляющим (привязка «чат→город», а не «человек→город»).
- Отклонить = освободить заявку (одна операция, не отдельные «отклонить» и «отменить»).
- Отклонение возможно только до одобрения.
- VK-код заморожен как референс, не удаляется.

## Точки отката (git)
- `6c613b4` — рабочее ядро: VK-прототип + начало правок под MAX.