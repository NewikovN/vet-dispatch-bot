# PROGRESS.md — текущая стадия проекта

> Обновлять после каждого крупного блока. Это точка входа в контекст для нового чата.

## Где мы сейчас
Весь основной функционал MAX-бота + блок отчётов готовы и проверены — сквозным живым тестом (онбординг, настройка направлений, заявки, захват, одобрение/отклонение, закрытие) и на seed-данных (`/отчет` со всеми фильтрами). VK-слой удалён, проект целиком на MAX. Следующий крупный блок — деплой.

## Транспорт MAX — проверено и работает
- Токен бота живой, домен `platform-api2.max.ru` отвечает.
- Сертификат Минцифры (`certs/russian_trusted_root.pem`) подключён через `NODE_EXTRA_CA_CERTS`, TLS проходит.
- Библиотека `@maxhub/max-bot-api` установлена.
- Бот: имя «Мокрый Нос», прошёл модерацию.

## Готово (ядро)
- Модели, роли, права (`domain/models.ts`) — обновлены под статус `approved`, добавлены `canApprove`, `canReject`, `canSeeDetails`.
- Жизненный цикл заявки `open → taken → approved → closed` + отклонение — реализован целиком, включая закрытие.
- Репозиторий заявок: создание, атомарный захват, одобрение, отклонение, закрытие от статуса `approved` (`db/requestsRepo.ts`).
- Схема БД: заявки (+ `approved_at`, `manage_message_id`), пользователи, `city_chats` (два чата на город).
- Репозиторий чатов по городам (`db/cityChatsRepo.ts`).
- Порт `Messenger`: `sendManageCard`, `editManageCard` добавлены и используются.
- Сервис `requestService.ts`:
  - `publishRequest` — нейтральная карточка в рабочий чат по городу;
  - `takeRequest` — атомарный захват БЕЗ выдачи контактов, нейтральная перерисовка рабочей карточки, детальная карточка в управленческий чат;
  - `approveTake` — проверка `canApprove`, `taken → approved`, обновление управленческой карточки, **единственное место, откуда контакты клиента уходят врачу в личку**;
  - `rejectTake` — проверка `canReject`, `taken → open`, обе карточки возвращаются в нейтральное состояние;
  - `startClosing`/`finishClosing` — закрытие возможно только из статуса `approved`, обновляются обе карточки (рабочая нейтрально, управленческая с суммой чека).
- Тест атомарного захвата (`test-claim.ts`) — проходит.
- VK-слой (`src/adapters/vk`, `src/app.ts`) удалён из репозитория — референс исчерпан, проект целиком на MAX.
- `pnpm exec tsc --noEmit` проходит с нулём ошибок (весь `src/` — живой код, исключений в `tsconfig` больше нет).
- MAX-адаптер целиком:
  - `adapters/max/cardView.ts` — две карточки: рабочая (`renderCardText`/`renderWorkKeyboard`, нейтральная, без врача и суммы) и управленческая (`renderManageCardText`/`renderManageKeyboard`, с деталями);
  - `adapters/max/MaxAdapter.ts` — реализует порт `Messenger` через `@maxhub/max-bot-api` (`sendMessageToChat`/`sendMessageToUser`/`editMessage`/`answerOnCallback`);
  - `app-max.ts` — основной поток: онбординг (`bot_started`), приём заявки/захват/одобрение/отклонение/закрытие через события (`message_created` в личке, `message_callback` для кнопок).
- Транспорт проверен вживую: `bot_started`, личка, группа, бот добавлен в группу с правами администратора — события доходят.
- Команды директора в `app-max.ts`: `/люди`, `/роль`, `/уволить`, `/города`, `/помощь`; `/привязать <Город> работа|управление` — прямо в групповом чате, chatId берётся из события (руками не вводится).
- Выбор направления при создании заявки — динамическими кнопками из `city_chats`, а не текстом: показываются только полностью настроенные направления (заданы ОБА чата); новое направление появляется в кнопках сразу после `/привязать`, без правок кода. Защита от ввода текста до выбора города. Проверено вживую.
- Баг с недоисчезающей клавиатурой при `editMessage` найден и исправлен (`attachments: kb ? [kb] : []`, не `undefined`) — кнопки корректно пропадают при смене статуса карточки.
- Экспорт заявок в Excel (`domain/exportService.ts`, `exceljs`) — платформо-независимый, не импортирует из `adapters/`; контакты клиента в отчёт НЕ включены (ждёт решения заказчика, ФЗ-152).
- Отправка файла в MAX (`MaxAdapter.sendDocument`) — ручная multipart-заливка через `bot.api.raw`, в обход `Api.uploadFile()`: баг библиотеки `@maxhub/max-bot-api@0.2.5` — для `type:'file'` заливка может уйти по video-пути (`uploadFromStream`/`uploadRange`) с преждевременным токеном, MAX отвечает `400 Missing token in video attachment`. Обход не трогает библиотеку, только `MaxAdapter`.
- Команда `/отчет` (директор, личка) с кнопочными фильтрами: «Полный» / «По направлению» / «По дате» / «Направление + дата» (двойной фильтр, город запоминается в состоянии ожидания периода). Текстовые аргументы `/отчет <город|период>` оставлены для совместимости.
- Периоды: один месяц (`ГГГГ.ММ`/`ГГГГ-ММ`) и диапазон месяцев включительно (`ГГГГ.ММ-ГГГГ.ММ`), границы строятся в МСК.
- `domain/datetime.ts` — единый модуль дат/пояса: `MSK_OFFSET_HOURS`, `parsePeriod`, `formatMsk`, `monthRangeMsk`/`monthRangeSpanMsk`. Даты в самой Excel-таблице — читаемые (`ДД.ММ.ГГГГ ЧЧ:ММ`, МСК), не «сырой» ISO UTC.
- `scripts/seed.ts` — dev-скрипт наполнения тестовыми данными; работает ТОЛЬКО с отдельной `data-seed.db` (защита от случайного запуска на боевой базе), путь к БД параметризован через `DB_PATH` в `db/index.ts`.

## В работе
- (пусто — блок отчётов завершён и проверен)

## Дальше (не начато)
- Решение заказчика: включать ли контакты клиента в Excel-отчёт (сейчас сознательно не включены).
- Деплой: webhook-обёртка (Express), VPS Beget, nginx, certbot, сертификат Минцифры на сервере, systemd, бэкапы, мониторинг.
- Опционально (по желанию заказчика): прочерки/пояснения вместо пустых ячеек в отчёте (сейчас пустая заявка = пустая ячейка).

## Важные решения (не переигрывать без причины)
- Один бот на все города (лимит MAX — 5 ботов на организацию; «бот на город» отвергнут).
- Два чата на город вместо личек управляющим (привязка «чат→город», а не «человек→город»).
- Отклонить = освободить заявку (одна операция, не отдельные «отклонить» и «отменить»).
- Отклонение возможно только до одобрения.
- VK-прототип использовался как референс на этапе переноса логики на MAX; после переноса — удалён из репозитория (проект целиком на MAX).
- Разработка MAX-бота ведётся на Long Polling (официально разрешён для разработки; библиотека `@maxhub/max-bot-api` умеет только его). В production MAX Long Polling не рекомендует — нужен Webhook.
- Библиотека webhook НЕ поддерживает (проверено разведкой пакета). Поэтому на этапе деплоя — отдельный блок работы: Express-обёртка принимает POST от MAX, nginx даёт HTTPS, подписка регистрируется через `POST /subscriptions`. Это ~день работы на деплое, домен и адаптер сообщений при этом не меняются.
- Домен для webhook: поддомен сайта заказчика (напр. `bot.клиника.ru`) → указывает на VPS. Отдельный домен покупать не нужно, если у заказчика есть свой.

## Точки отката (git)
- `6c613b4` — рабочее ядро: VK-прототип + начало правок под MAX.
- `5b54b5f` — ядро под новую модель: одобрение/отклонение, два чата, контакты после одобрения; VK исключён из сборки.
- `fdd6ac1` — MAX-адаптер: карточки, MaxAdapter, основной поток событий (онбординг, захват, одобрение, закрытие).
- `c7b80de` — выбор направления кнопками из БД: динамические направления, защита от опечаток в городах.